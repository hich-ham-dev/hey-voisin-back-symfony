{% block nav %}
	<nav class="navbar navbar-expand-lg shadow bg-success-subtle bg-gradient rounded-bottom-4 py-3">
		<div class="container-fluid">
			<a class="navbar-brand fw-bold text-light p-3 fs-2 bg-secondary bg-gradient rounded-pill" href="{{ path('app_main') }}">Hey_Voisin, le Back-Office</a>
			{% if is_granted("IS_AUTHENTICATED_FULLY") %}
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>
				<div class="collapse navbar-collapse ms-5" id="navbarNavDropdown">
					<ul class="navbar-nav">
						<li class="nav-item">
							<a class="nav-link active fw-bold" aria-current="page" href="{{ path('app_main') }}">Accueil</a>
						</li>
						<li class="nav-item">
							<a class="nav-link fw-bold" href="{{ path('app_post_index') }}">Publications</a>
						</li>
						<li class="nav-item">
							<a class="nav-link fw-bold" href="{{ path('app_category_index') }}">Catégories</a>
						</li>
						<li class="nav-item">
							<a class="nav-link fw-bold" href="{{ path('app_user_index') }}">Utilisateurs</a>
						</li>
						<li class="nav-item">
							<a class="nav-link fw-bold" href="{{ path('app_city_index') }}">Villes</a>
						</li>
						<li class="nav-item">
							<a class="nav-link fw-bold" href="{{ path('app_avatar_index') }}">Avatars</a>
						</li>
						<li class="nav-item">
							<a class="nav-link fw-bold" href="{{ path('app_comment_index') }}">Commentaires</a>
						</li>
						<li class="nav-item">
							<a class="btn btn-outline-danger shadow rounded-pill fw-bold ms-3" href="{{ path('app_logout') }}">Déconnexion</a>
						</li>
					</ul>
			{% endif %}
			</div>
		</div>
	</nav>
{% endblock %}
