{% extends 'base.html.twig' %}

{% block title %}Liste des villes{% endblock %}

{% block body %}
<div class="container col-9">
    
    <h1 class="mt-5">Liste des villes <a href="{{ path('app_city_new') }}" class="btn btn-success btn-sm rounded-pill">Ajouter une ville</a></h1>

    <table class="table table-bordered table-striped table-sm">
        <thead class="table-dark">
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Zipcode</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for city in pagination %}
            <tr>
                <td>{{ city.id }}</td>
                <td>{{ city.name }}</td>
                <td>{{ city.zipcode }}</td>
                <td>
                    <div class="dropdown">
                        <button class="btn btn-outline-primary m-1 btn-sm rounded-pill dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Actions
                        </button>
                        <ul class="dropdown-menu rounded-4 p-1">
                            <li><a class="dropdown-item" href="{{ path('app_city_show', {'id': city.id}) }}" >Voir</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_city_edit', {'id': city.id}) }}" >Modifier</a></li>
                            <li><a class="dropdown-item bg-danger rounded-pill text-light" href="{{ path('app_city_delete', {'id': city.id}) }}">Supprimer</a></li>
                        </ul>
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    {{ knp_pagination_render(pagination) }}
</div>
{% endblock %}
